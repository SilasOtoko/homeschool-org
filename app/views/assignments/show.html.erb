<%= image_tag @assignment.assignment_url if @assignment.assignment? %>
<%= link_to 'Edit', edit_student_assignment_path(@student, @assignment) %>
<%= link_to 'Delete', student_assignment_path(@student, @assignment), :confirm => 'Are you sure?', :method => :delete %>